# Explicit development-only service checks. Copy the export commands from the
# README into your shell, or load this file with a dotenv-aware process runner.
NOC_SERVICE_TARGETS='[{"name":"NOC Agent Health","type":"http","url":"http://127.0.0.1:8000/health"},{"name":"Local SSH","type":"tcp","host":"127.0.0.1","port":22}]'

# Per-target connection/request timeout; valid range: 0.1 to 30 seconds.
NOC_SERVICE_CHECK_TIMEOUT_SECONDS=2

